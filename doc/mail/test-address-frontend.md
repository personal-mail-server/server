# 테스트용 메일 주소 프론트엔드 문서

## 문서 목적
이 문서는 테스트용 메일 주소 목록/상세/생성 프론트엔드 화면의 구성과 사용자 흐름을 정의한다.

---

## 기준 문서
- `doc/mail/pr-split/07-frontend-read-create.md`
- `doc/mail/server-requirements.md`
- `doc/mail/test-address-create-api.md`
- `doc/mail/test-address-read-api.md`
- `doc/mail/test-address-generate-api.md`

---

## 구현 위치
- `code/frontend/index.html`
- `code/frontend/styles.css`
- `code/frontend/app.js`

---

## 화면 구성

### 로그인 전
- 기존 로그인 폼을 그대로 사용한다.

### 로그인 후
- 세션 요약 영역을 유지한다.
- 아래에 다음 3개 영역을 표시한다.
  - 주소 목록 패널
  - 상세 정보 패널
  - 새 주소 생성 패널

---

## 사용자 흐름

### 목록 조회
- 로그인 성공 직후 주소 목록을 자동으로 불러온다.
- 사용자는 새로고침 버튼으로 목록을 다시 불러올 수 있다.

### 상세 조회
- 목록에서 주소를 클릭하면 상세 패널에 선택 주소 정보가 표시된다.

### 직접 입력 생성
- 사용자는 이메일을 직접 입력해 생성할 수 있다.
- 생성 성공 시 목록과 상세가 즉시 갱신된다.

### 후보값 생성
- 사용자는 후보 생성 버튼으로 추천 이메일을 받아 입력창에 채울 수 있다.
- 후보값은 그대로 사용하거나 수정 후 생성할 수 있다.

---

## 상태 표시
- 기존 상태 패널을 그대로 사용한다.
- 목록 로딩, 후보 생성, 생성 성공, 생성 실패, 상세 조회 실패를 모두 상태 패널에 표시한다.

---

## 제외 범위
- 수정 UI
- 삭제 UI
- 복구 UI

---

## 정리
현재 프론트엔드는 기존 로그인 단일 페이지를 확장해, 로그인 후 테스트용 메일 주소 목록/상세/생성 흐름을 한 화면에서 검증할 수 있도록 구성한다.
